import { BrojeviDirective } from './brojevi.directive';

describe('BrojeviDirective', () => {
  it('should create an instance', () => {
    const directive = new BrojeviDirective();
    expect(directive).toBeTruthy();
  });
});
